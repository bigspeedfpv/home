---
import "../styles/global.css";
import NavButton from "../components/NavButton.astro";

const { title } = Astro.props;

const navButtons = [
  { title: "home", route: "/" },
  { title: "blog", route: "/blog" },
  { title: "linkedin", route: "https://www.linkedin.com/in/andrewbrower" },
  { title: "github", route: "https://github.com/bigspeedfpv" },
];
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title ? `${title} - Andrew Brower` : "Andrew Brower"}</title>
	</head>
	<body class="bg-sepia flex justify-center">
    <div class="p-12 w-full max-w-[750px]">
      <header class="my-8 flex flex-row justify-between items-center flex-wrap">
        <h1 class="font-bold text-2xl mr-4">Andrew Brower</h1>
        <div class="flex gap-4 flex-wrap">
          {navButtons.map(({title, route}) => <NavButton href={route}, text={title} />)}
        </div>
      </header>
      <main>
    		<slot />
      </main>
    </div>
	</body>
</html>
